// No loading UI for page transitions — data is served instantly from AppDataContext in-memory store.
// The framer-motion slide animation in AppShell handles visual transition feedback.
export default function Loading() {
  return null
}
